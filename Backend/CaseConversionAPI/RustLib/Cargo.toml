[package]
name = "rust_lib"
version = "0.1.0"
edition = "2024"

[lib]
# Combining both rlib (for tests) and cdylib (for FFI)
crate-type = ["rlib", "cdylib"] 

[dependencies]
# Your dependencies go here